package day03_Variables;

public class Square {

    public static void main(String[] args) {
        //DataType variableName = Data;
        // double is always for decimals and int is for whole numbers
        // variables makes it easy to run information without having to write all over again. when variable is assigned, it is automatically stored and put in file

        double  side = 3.5;
        double area = side * side;
        double perimeter = side * 4;
        System.out.println("area = " + area);
        System.out.println("perimeter = " + perimeter);


    }

}
 /*1. Create a class named Square, write a program that can calculate the area & perimeter of any given square
                        side (3.5)

                        area = side * side;
                        perimeter = 4 * side
         */